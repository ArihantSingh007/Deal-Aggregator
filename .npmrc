# Some dependencies (notably next-auth@4) haven't formally widened their peerDependencies
# to include React 19 yet, even though they work correctly with it. Without this, npm 7+
# can fail the whole install with an ERESOLVE error. legacy-peer-deps relaxes that check
# to a warning instead of a hard failure.
legacy-peer-deps=true
